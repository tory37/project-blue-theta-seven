{
  "mcpServers": {
    "trello": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-trello"
      ],
      "env": {
        "TRELLO_API_KEY": "{{TRELLO_API_KEY}}",
        "TRELLO_TOKEN": "{{TRELLO_TOKEN}}"
      }
    }
  }
}
